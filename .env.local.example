# Copy to .env.local and populate with actual keys before running locally
VITE_GEMINI_API_KEY="your-google-gemini-api-key"
# Optional: override default model (defaults to gemini-2.5-pro)
VITE_GEMINI_MODEL="gemini-2.5-pro"
